<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.banking">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".UI.UserData"></activity>
        <activity android:name=".UI.SendToUser" />
        <activity android:name=".UI.TransactionHistory" />
        <activity android:name=".UI.UsersList" />


        <activity android:name=".UI.HomeScreen" >

            <intent-filter>

            <action android:name="android.intent.action.MAIN"/>

            <category android:name="android.intent.category.LAUNCHER"/>

        </intent-filter>


        </activity>
        <activity android:name=".ListAdapters.UserListAdapter" />
        <activity android:name=".ListAdapters.TransactionHistoryAdapter" />
        <activity android:name=".ListAdapters.SendToUserAdapter" />
        <activity android:name=".DB.UserHelper" />
        <activity android:name=".DB.UserContract" />
        <activity android:name=".DB.TransactionHelper" />
        <activity android:name=".DB.TransactionContract" />
    </application>

</manifest>